# The ID is the Spec name and generated or provided but the PK is the name. The ID will be used for delete and updates.
# All get(s) are per namespace. It is used ID + namespace. This is contolled by the controllers. The service provides one get API QBE. A user will not have access to a query for a different namespace.
# The data services APIs are all POST, PUT or PATCH. This means that all of them expects a body.
# The data services are the Gaming and Social APIs.
